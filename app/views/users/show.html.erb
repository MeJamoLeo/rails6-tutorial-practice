<%= provide(:title, @user.name) %>
<div class="row">
    <aside class="col-cd-4">
        <section class="user_info">
            <h1>
                <%= gravatar_for(@user, sise: 20) %>
                <%= @user.name %>
            </h1>
        </section>
    </aside>
</div>