<% if @feed_items.any? %>
<ol class="microposts">
    <%= render @feed_items %>
    <%#
    @feed_itemsはMicropostクラスを持っているため，
    Micropostのパーシャルを呼び出すことができる. _micropost.html.erb
    %>
</ol>
<%= will_paginate @feed_items,params: { controller: :static_pages, action: :home } %> %>
<% end %>